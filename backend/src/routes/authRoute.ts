//@ts-nocheck
import { Router } from "express";

const router = Router();

router.post("/register", );
router.patch("/update", );
router.delete("/delete", );
router.post("/login", );
router.post("/logout", );